const NAME_SPACE = `free-components-autoComplateInput`
const CONSTANTS = {
    CLASS_NAME: {
        SUGGESTIONS_CONTAINER: `${NAME_SPACE}-suggestions-container`,
        SUGGESTIONS_ITEM: `${NAME_SPACE}-suggestions-item`
    }
}

export default CONSTANTS
