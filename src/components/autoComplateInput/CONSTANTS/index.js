const NAME_SPACE = `free-components-autoComplateInput`
const CONSTANTS = {
    CLASS_NAME: {
        WRAPPER: `${NAME_SPACE}-wrapper`,
        LOADING: `${NAME_SPACE}-loading`,
        SUGGESTION_LIST: `${NAME_SPACE}-suggestions-list`,
        SUGGESTION_ITEM: `${NAME_SPACE}-suggestions-item`,
        ICONFONT: `${NAME_SPACE}-iconfont`,
        ICONFONT_LOADING: `${NAME_SPACE}-iconfont-loading`
    }
}

export default CONSTANTS
